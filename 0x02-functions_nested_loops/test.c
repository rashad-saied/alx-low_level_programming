#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int r, y, u;
	r = _islower('H');
	y = _islower('o');
	u = _islower(108);

	_putchar(r);
	_putchar(y);
	_putchar(u);
	return (0);
}

